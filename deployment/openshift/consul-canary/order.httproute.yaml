apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: httproute-order-service
  namespace: consul
spec:
  parentRefs:
  - name: api-gw-demo-app
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /canary2/
    backendRefs:
    - kind: Service
      name: order
      namespace: order
      port: 8080
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          replacePrefixMatch: /
          type: ReplacePrefixMatch

